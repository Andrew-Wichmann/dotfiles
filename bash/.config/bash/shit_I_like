export PATH=$PATH:$HOME/.local/bin
alias vim=nvim
alias screen_recorder='wf-recorder -f recording.mkv && ffmpeg -i recording.mkv -vf "fps=15,scale=1080:-1:flags=lanczos" -c:v gif recording.gif && rm recording.mkv'
